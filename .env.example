# Build Configuration
export DOCKER_BUILDKIT=1

# Development Configuration
export UV_INSTALL_DEV=1
export NODE_ENV=development

# Django Core Settings
export DJANGO_SETTINGS_MODULE=config.settings.dev  # Use config.settings.prod for production
export DJANGO_DEBUG=1  # Set to 0 in production
export DJANGO_SECRET_KEY="django-insecure-6ihto=*y%1dpee(w+kx@%7z13dma#f9#(c4&bph=h3epqrvh"  # CHANGE IN PRODUCTION!
export DJANGO_ALLOWED_HOSTS="localhost,127.0.0.1"  # Comma-separated list; add your domains for production

# Database Configuration
export POSTGRES_USER=djangouser
export POSTGRES_DB=djangodb
export POSTGRES_PASSWORD=  # Set in production!
export DATABASE_URL="postgres://djangouser@db:5432/djangodb"  # Format: postgres://user:password@host:port/dbname

# Docker Configuration
export DOCKER_APP_HEALTHCHECK_TEST=/bin/true

# Frontend Asset Configuration
export DJANGO_VITE_DEV_MODE=true  # Set to false in production
export DJANGO_VITE_DEV_SERVER_PORT=5175
export DJANGO_VITE_DEV_SERVER_HOST=0.0.0.0

# Production Security (uncomment and set these in production)
# export DJANGO_SECURE_SSL_REDIRECT=1
# export DJANGO_LOG_LEVEL=INFO
# export DJANGO_DEFAULT_FROM_EMAIL=noreply@example.com

# Cache Configuration (for production)
# export REDIS_URL=redis://redis:6379/0
